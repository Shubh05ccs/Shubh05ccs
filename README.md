# Shubh05ccs — GitHub profile config

Config files for my GitHub profile. This repository previously had no files; I added this README so integrations (like Claude) can access repository contents.

What this repo contains:
- (Initial) README.md — short description and attach instructions.

How to attach this repo to Claude
1. In Claude, open Integrations → GitHub and confirm your account is connected (you said it already is).
2. Refresh the repository list or re-open the "Attach repository" flow; select the repository "Shubh05ccs/Shubh05ccs".
3. If Claude still shows an error, try one of the following:
   - Re-authorize the Claude GitHub app in GitHub: Settings → Applications → Authorized OAuth Apps → (find Claude) → Revoke and Re-authorize with repo access.
   - Ensure the default branch is "main" and contains at least one file (this README has been added).
   - If you prefer not to use the integration, zip the repo and upload the zip file directly to Claude.

If you want, I can add more files (example profile README, .github/profile/README or links) or push the specific config files you plan to use with Claude.